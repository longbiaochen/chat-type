# Changelog

## 0.1.0

- Public launch defaults now use the OpenAI-compatible transcription path
- AI cleanup is enabled by default with OpenAI-compatible chat completions defaults
- Added runtime setup validation for missing API keys and cleanup configuration
- Added explicit clipboard fallback messaging when Accessibility permission is missing
- Added release packaging metadata sourced from `version.env`
- Added public release zip generation for `dist/VoiceDex-0.1.0-macos-arm64.zip`
